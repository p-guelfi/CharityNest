<%# needs a report object as an argument %>
<div class="<%= report.report_type == "Evaluation" ? 'report card-report-teaser report-evaluation' : 'report card-report-teaser report-article' %>">
  <%= render "reports/report_content_essentials", report: report %>
</div>
