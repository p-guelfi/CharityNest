<h2><%= donation.charity_project.name %></h2>
<% if donation.recurrent? %>
  <p>Monthly Donation: <%= donation.amount %> €</p>
  <p>since
<% else %>
  <p>You donated: <%= donation.amount %> €</p>
  <p>on
<% end %>
  <%= donation.created_at.strftime("%b %d, %Y") %></p>
