<h1 class="discussions-heading">Discussions for <%= link_to @charity_project.name, charity_project_path(@charity_project) %></h1>

<div class="button-container">
  <%= link_to 'New Discussion', new_charity_project_discussion_path(@charity_project), class: 'btn-primary' %>
</div>

<div class="discussions-list">
  <% @discussions.each do |discussion| %>
    <div class="discussion-card">
      <div class="discussion-card-header">
        <div class="discussion-title">
          <h3><%= link_to discussion.title, charity_project_discussion_path(@charity_project, discussion) %></h3>
        </div>
        <div class="discussion-content">
          <p><%= discussion.description %></p>
        </div>
      </div>

      <div class="discussion-info">
        <% if discussion.user.present? %>
          <% name = "#{discussion.user.first_name} #{discussion.user.last_name}".strip.presence || discussion.user.email %>
          <p class="user-info"> by <%= name %> | <%= time_ago_in_words(discussion.created_at) %> ago</p>
        <% else %>
          <p class="user-info">Anonymous | <%= time_ago_in_words(discussion.created_at) %> ago</p>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
