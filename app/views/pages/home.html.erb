<div class="hero-section">
  <div class="hero-content">
    <h1 class="hero-title">Title Title</h1>
    <p class="hero-description">Description Description Description</p>
    <button class="button">Call to Action</button>
  </div>
</div>

<section class="categories">
  <h2>Categories</h2>
  <div class="category-cards">
    <%= render 'shared/category_card', title: "Hunger" %>
    <%= render 'shared/category_card', title: "Climate Change" %>
    <%= render 'shared/category_card', title: "Disaster Relief" %>
    <%= render 'shared/category_card', title: "Education" %>
    <%= render 'shared/category_card', title: "Health" %>
    <%= render 'shared/category_card', title: "Water & Sanitation" %>
  </div>
</section>

<%= render 'shared/footer' %> <!-- Render the footer section -->
